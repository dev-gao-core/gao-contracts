{
  "name": "gao-id-worker-contracts",
  "version": "0.1.0-dev",
  "private": true,
  "description": "GaoDomainDeposit escrow contract + Hardhat scaffolding. Companion package to gao-id-worker; deployed independently.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:base-sepolia": "hardhat run scripts/deploy.ts --network baseSepolia",
    "deploy:base": "hardhat run scripts/deploy.ts --network base",
    "verify:base-sepolia": "hardhat verify --network baseSepolia",
    "verify:base": "hardhat verify --network base",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "@types/chai": "^4.3.16",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.12.0",
    "chai": "^4.4.1",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.0",
    "hardhat": "^2.22.0",
    "typescript": "^5.4.0"
  }
}
